package stuff;

import flixel.FlxSprite;

class Ball extends FlxSprite {
    var type:String = '';
    var texture:String = '';

    public function setType(v:String) {
        switch(v) {
            default:
                
        }
    }
}
